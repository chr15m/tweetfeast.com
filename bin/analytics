#!/bin/sh

mkdir -p build/analytics
goaccess -f logs/*access.log --log-format='%h %^[%d:%t %^] "%r" %s %b "%R" "%u"' --time-format="%H:%M:%S" --date-format="%d/%b/%Y" --ignore-crawlers > ./build/analytics/index.html; sleep 10800
